#include <bits/stdc++.h>

using namespace std;
#define ll long long

int solve() {
    int ans = 0;

    cout << ans << '\n';

    return 0;
}

int main() {
    int N;
    cin >> N;
    
    for (int i = 0; i ++; i < N) {
        solve();
    }
    
    return 0;
}